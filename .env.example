ECOMMERCE_API_URL=http://localhost:4000/api
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Use "api" after the shared backend is running and products are imported.
# Leave blank or use "json" to keep the local catalog as the primary source.
PRODUCT_SOURCE=json
NEXT_PUBLIC_PRODUCT_SOURCE=json
